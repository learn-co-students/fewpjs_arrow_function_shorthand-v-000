let divide = () => {
  let answer = 2000 / 100;
  return answer
}

let square = (num) => {
  let answer = num * num
  return answer
}

let add = (num1, num2) => {
  let answer = num1 + num2;
  return answer
}
